<div class="cont-<%= message.user.id %>">
  <div class="message-box msg-<%= message.user.id %> " >
    <div class=" d-flex align-items-center">
      <small>
        <strong><%= message.user.full_name %></strong>
        <i><%= time_ago_in_words(message.created_at) %></i>
      </small>
    </div>
    <%= message.content %>
  </div>
</div>
